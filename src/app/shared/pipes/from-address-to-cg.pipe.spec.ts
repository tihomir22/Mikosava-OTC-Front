import { FromAddressToCgPipe } from './from-address-to-cg.pipe';

describe('FromAddressToCgPipe', () => {
  it('create an instance', () => {
    const pipe = new FromAddressToCgPipe();
    expect(pipe).toBeTruthy();
  });
});
